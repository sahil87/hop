# cli — memory

| File | Topic |
|---|---|
| [subcommands](subcommands.md) | What each `hop` subcommand does, exit codes, external-tool failure messages, ad-hoc URL clone, auto-init-on-write (write-commands self-bootstrap a missing config), shell-init shim, hidden `help-dump` JSON help-tree contract |
| [match-resolution](match-resolution.md) | Case-insensitive substring + fzf fallback algorithm; group-suffix disambiguation in the picker |
