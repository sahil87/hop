- [ ] [t2hs] 2026-05-03: how to decide where to place repo.yaml? An env var holds it?
- [ ] [7lhk] 2026-05-03: repo init zsh - sounds misleading. repo completions zsh - sounds better
- [ ] [outb] 2026-05-03: Have a subcommnd to sync all repos (pull + push) - even just pull or push - both all and specific variants
- [ ] [wett] 2026-05-04: Refactor `cloneURL` (src/cmd/hop/clone.go:107) — ~68-line function mixes resolution, state classification, cloning, registration, and stdout. Split per-state arms into helpers (`handleMissing`, `handleAlreadyCloned`) with a shared `finishClone(cmd, configPath, g, groupName, url, noAdd, noCD, path)` post-step.
- [ ] [qwrd] 2026-05-04: `buildPickerLines` (src/cmd/hop/where.go) does not disambiguate when two URLs in the *same* group share a derived `Name` — both render as `name [group]` identically. Cross-group collisions are handled. Consider a tiebreaker (e.g., include `org` in the suffix when intra-group collision occurs).
- [ ] [k1di] 2026-05-04: `registerURL` (src/cmd/hop/clone.go:204) takes both `g *config.Group` and `groupName string`; `g.Name` is authoritative. Pick one source of truth.
- [ ] [bhrt] 2026-05-04: `-R` integration coverage gaps in src/cmd/hop/integration_test.go — add tests for multi-arg child command (`hop -R <name> git status --short`), non-zero exit propagation (`hop -R <name> false` → exit 1), and missing-target stderr (`hop -R nonexistent ls` → exit 1 with resolution error). (Updated from `-C` after #9 renamed the flag.)
- [ ] [avsb] 2026-05-04: `hop path <name>` surfaces cobra's `accepts at most 1 arg(s), received 2` rather than an "unknown subcommand" message. Consider a `cobra.OnUnknownCommand` handler or stricter root args to give a clearer hint to users typing the v0.0.1 form.
- [ ] [9wp6] 2026-05-04: `proc.RunForeground` (src/internal/proc/proc.go) lacks a focused stdout-capture unit test. The `-C` integration test exercises stdio inheritance end-to-end, but a dedicated test (e.g., `TestRunForegroundEcho`) would be useful for documentation and isolation.
- [ ] [8yhz] 2026-05-04: Add a `tools:` allowlist section to `hop.yaml` so the binary (not just the shim) can dispatch `hop <tool> <repo>` as tool-form, with optional aliases (e.g., `c: cursor`, `v: nvim`). Complements the shim-only tool-form that ships with the `-R` change. Inspired by `wt open`'s curated app catalog (fab-kit `src/go/wt/internal/worktree/apps.go`). Decisions to make: `command -v` vs curated detection (per-OS bundle/desktop-file lookups like wt); whether `tools:` lives at top level or per-group; whether aliases are case-insensitive; `hop tools` listing subcommand for discoverability.
- [ ] [06jc] 2026-05-04: Tab-completion on `$2` for shim tool-form (`hop cursor <TAB>`) does not work today. Cobra's auto-generated completion only sees the binary's `cobra.MaximumNArgs(1)` root and returns no candidates once `$1` is consumed. Needs a custom zsh+bash completion wrapper that detects `${words[1]}` is a binary on PATH and `CURRENT == 3` (zsh) / `COMP_CWORD == 2` (bash), then injects repo names. Also implies: `hop -R <TAB>` could complete repo names if `-R` were a cobra flag (currently parsed pre-cobra in `extractDashR`), but that's a deeper refactor since cobra would also try to parse the child argv. Flagged by Copilot review on PR #9. Until fixed, users tab-complete via the explicit `hop -R <TAB>`-less path: type the repo name fully or use `hi where <TAB>` / `hop where <TAB>`.
- [x] [jr5f] 2026-06-02: Add a build-time 'help-dump' step that emits hop's CLI help tree as help/hop.json and PRs it into sahil87/shll.ai (the shll.ai landing site renders it as an expandable 'Command reference' on the hop tool page). CONTRACT (frozen — copy the reference sample at sahil87/shll.ai path help/wt.json): JSON shape is {tool, version, captured_at (ISO-8601 UTC), schema_version: 1, root: Node} where Node = {name, path (full invocation e.g. 'hop where'), short (one-line desc), usage, text (the RAW -h output byte-for-byte, newlines preserved), commands: Node[] (recursive; empty array = leaf)}. PRODUCER (hop is Cobra/Go, binary 'hop', main at src/cmd/hop): walk the cobra command tree programmatically (rootCmd.Commands() recursively), NOT regex-parsing -h text; per node capture cmd.Name / cmd.CommandPath() / cmd.Short / cmd.UseLine() and cmd.UsageString() (or Long+UsageString) as 'text'. FILTER OUT cobra's auto-generated 'completion' and 'help' subcommands and any cmd.Hidden==true. VERSION: read from the built binary (rootCmd.Version / ldflags) — do NOT hardcode. NOTE hop's pre-cobra -R / tool-form argv handling (extractDashR) lives outside the cobra tree — decide whether to document those in root 'text' manually or leave to the README. PUSH: in CI after build, run the dump, write help/hop.json, validate it parses, then open a PR into sahil87/shll.ai using the existing repo secret SHLLAI_TOKEN (contents + pull-request write) with auto-merge enabled (PR, not direct push to main, to avoid the multi-repo push race). This is hop's slice of a 7-tool rollout; the shll.ai site-side consumer (Astro loader + reference UI) is tracked separately in the shll.ai repo.
- [ ] [m4kp] 2026-06-03: Bump pinned GitHub Actions off Node.js 20 before the forced cutover. Every Release run annotates: "Node.js 20 actions are deprecated … forced to run with Node.js 24 by default starting June 16th, 2026; Node 20 removed from the runner Sept 16th, 2026." Affected pins in `.github/workflows/release.yml`: `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4`, `actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5`, `softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe # v2`. Re-pin each to a current Node-24-capable release SHA (keep the `# vN` comment convention and the SHA-pinning policy noted in docs/memory/build/release-pipeline.md § Action SHAs — that doc says SHAs track run-kit's release.yml, so bump there in lockstep or justify the deviation). Low risk, but do it before 2026-06-16 to avoid surprise behavior on the forced switch.
- [ ] [cmp7] 2026-06-08: Fix hop's shell-completion bugs (deferred from the grammar-shim refactor 260608-gyo0). Three problems in `src/cmd/hop/repo_completion.go`: (1) tab-completion synchronously shells out to `wt list --json` with a 5s timeout, BLOCKING the interactive shell on a slow/hung repo; (2) worktree completion breaks silently on ambiguous prefixes (e.g. `web<TAB>` for `webapp` fails to match the `/`-worktree boundary, `repo_completion.go:149`); (3) completion errors are silently swallowed, so it "just stops working" with no diagnostic. ROOT-CAUSE FIX likely belongs in `wt`, not hop: add a `--porcelain` (stable, fast, machine-readable) option to `wt list` so hop's completion (and `hop ls --trees`) can query worktrees cheaply without the fragile JSON-over-timeout dance. So this is a TWO-repo fix: `wt` gets `--porcelain`; hop's completion switches to it + stops blocking + surfaces errors. Coordinate with the `wt` repo (~/code/sahil87/wt). Related stale items: [06jc] (tool-form tab-completion), [8yhz] (tools: allowlist). NOTE: the grammar-shim refactor will have already done whatever completion changes the new grammar STRICTLY forces (keep cobra `__complete*` working under `--shim-plan`; pull/push/sync no longer complete as subcommands) — this item is the remaining BUG fixes only.
- [ ] [clc4] 2026-07-18: `hop rm` (and the hidden `hop config rm` alias) mutate `hop.yaml` — a destructive registry write — with NO consent gate on the direct `hop rm <name>` path (no `--yes`, no confirmation prompt), deferred from the toolkit-standards conformance audit (260717-fcvp). Toolkit principle №5 says destructive writes MUST require explicit consent per №1 (interactive confirm on a TTY, `--yes`/`-y` for automation, fast refusal when neither). The audit added `--dry-run` (the small additive half) but deferred consent because `hop rm <name>` is DOCUMENTED as immediate/non-interactive (see docs/memory/cli/subcommands.md: "no confirmation prompt") and the picker path is already interactive consent — adding a mandatory prompt/`--yes` changes that contract and needs a design decision. Decisions to make: does `hop rm <name>` warrant consent at all given it edits a registry entry (reversible, esp. when hop.yaml is version-controlled) and never deletes on-disk data? If yes: `--yes`/`-y` to satisfy №1's automation escape, a TTY-gated `Proceed? [y/N]` prompt (mirror `shll uninstall`, the toolkit's reference impl), and a no-TTY refusal naming `--yes`; reconcile with the existing HOP_WRAPPER/no-TTY (exit 3) machinery. Reference for the deferral: the fcvp conformance report's principles §5 entry.
- [ ] [qner] 2026-07-18: Add a root-level agent-entry file (`CLAUDE.md` and/or `AGENTS.md`) that POINTS AT the sahil87 toolkit standards (enumerated by `shll standards`) rather than restating them — deferred from the toolkit-standards conformance audit (260717-fcvp). Toolkit principle №10 (SHOULD): "Agent entry files (`CLAUDE.md`/`AGENTS.md`) point at these standards rather than restating them." hop ships neither today. Deferred (not fixed in fcvp) because authoring an agent-entry pointer file is documentation curation — which idioms/standards to point at, how much repo-development context to include — not a mechanical flag/stream/error fix, so it sits outside that change's small-additive fix boundary. Keep it a thin pointer (link `shll standards`, the shll.ai tool page, and `docs/specs/`), NOT a restatement — the whole point of №10 is agents read the canonical standard, not a drifting copy. Reference: the fcvp conformance report's principles §10 entry.
- [ ] [armh] 2026-07-18: Adopt the toolkit `skill` standard — add a `hop skill` subcommand that prints a stable, ≤150-line, static, agent-facing usage bundle to stdout, byte-identical to a canonical `docs/site/skill.md` (renders at /hop/skill on shll.ai for free). Deferred (NOT a violation yet) per the standard's own phased per-repo adoption clause ("No tool ships `skill` today… a tool without a `skill` subcommand is not yet in violation") and the fcvp intake's pre-decided disposition. When adopting: command name is exactly `skill` (not `agent`/`context`); stdout-only, stderr empty, exit 0; content is a usage briefing (when-to-use, capabilities map keyed to subcommands, composition patterns with wt/fzf/git per №7, stdout/stderr + exit-code contract, gotchas) — NOT a README clone or flag table (defer those to `-h`/`help-dump`); embed via the sync + drift-guard pattern `shll standards` established (committed embedded copy + sync script + a drift-guard test failing the build on divergence). This is `hop`'s slice of the 7-tool phased rollout. Reference: the fcvp conformance report's `skill` section names this as the deferral target.
